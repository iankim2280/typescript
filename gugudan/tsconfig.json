{
  // js와 ts파일을 같이 열수 있는 설정
  "compilerOptions": {
    "noImplicitAny": true,
    "noImplicitThis": true,
    "noImplicitReturns": true,
    "strict": true,
    "lib": ["DOM", "ES6", "ES2016", "ES2017", "ES2018", "ES2019", "ES2020"],
    "target": "ES6"
  },
  "exclude": ["*.js"]
  //   어떤 파일을 컴파일 할지 "include": ["lecture.ts"], "exclude": ["*.js"]
  // 여러 프로젝트에서 세세하게 설정이 다를때 사용 // extends
}
